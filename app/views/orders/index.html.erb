<% if !@orders %>
  <%= flash[:notice] = "You have no orders yet"%>
<% else %>
  <% @orders .each do |order| %>
    <%order.orderlines.each do |orderline|%>
      <%= orderline.quantity %>
      <%= orderline.product.name %>
      <%= orderline.product.specification.size %>
      <%= orderline.product.specification.color %>
      <%= orderliner.product.price %>
    <%end%>
    <%=order.make_subtotal%>
  <%end%>
<%end%>s

 <%= simple_form_for [@product, @specification, @image] do |f| %>
  <%= f.error_notification %>

  <%= f.input :photo %>
  <%= f.input :photo_cache, as: :hidden %>

  <%= f.button :submit %>
<% end %>

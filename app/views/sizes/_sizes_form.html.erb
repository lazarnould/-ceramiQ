 <%= simple_form_for [@product, @specification, @size] do |f| %>
  <%= f.error_notification %>

  <%= f.input :size, collection: ["XS", "S", "M", "L", "XL", "XXL"] %>
  <%= f.input :quantity %>

  <%= f.button :submit %>
<% end %>
